# Frontend for route finding

This is a frontend for a route finding web application.

## Set up

1. Create a .env file in root. Fill in corresponding values with reference to `.env.example` file.
2. run `npm install`

## Dev run

run command:

```
npm run dev
```

## Production Build

run command:

```
npm run build
```